<%- include("partials/header.ejs") %>
  <div class="chatSelectionDiv">
    <ul class="list-group">
      <% for(var x = chatRoom.length - 1; x >= 0; x--){ %>
        <li class="list-group-item list-group-item-action" onclick="window.location='/home/chat/<%= chatRoom[x].room_name %>';"><%= chatRoom[x].room_name %></li>
      <% } %>
    </ul>
  </div>
  <div class="chatDisplay">
    <img class="newRoomBtn" src="newProjectIcon.png" id="newRoom">
    <div id="newRoomDiv">
      <form action="/home/newRoom" method="POST">
        <div class="input-group mb-3">
          <label for="rName">Create New Room</label><br>
          <input type="text" class="form-control"  name="name" aria-label="Recipient's username" aria-describedby="button-addon2" id="rName" class="chatInp">
          <div class="input-group-append">
            <button class="btn btn-outline-danger" type="button" id="button-addon2">Create</button>
          </div>
        </div>
      </form>
    </div>
  </div>
  <script type="text/javascript" src="/newRoom.js"></script>
<%- include("partials/footer.ejs") %>
